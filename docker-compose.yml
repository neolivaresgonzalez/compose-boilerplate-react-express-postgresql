version: "3.9"
services:
  web:
    build: ./client
    ports:
      - "8000:5000"
    depends_on:
      - db
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: password_pg
    ports:
      - "5432:5432"